export type DiscoverChipSelection = Record<string, boolean>;

export type DiscoverScrollPosition = "left" | "right" | "mid" | "noscroll";

export interface DiscoverChipItem {
  key: string;
  label: string;
}
