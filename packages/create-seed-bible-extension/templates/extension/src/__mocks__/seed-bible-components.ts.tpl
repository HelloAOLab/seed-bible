// Test-time stand-in for "seed-bible/components" — see seed-bible.ts in
// this directory for why this exists. Empty by default; add a `vi.mock`
// override in your own tests for whichever component(s) you actually use.
export {};
